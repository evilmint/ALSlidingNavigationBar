use_frameworks!

target 'ALSlidingNavigationBar_Example' do
  pod 'ALSlidingNavigationBar', :path => '../'

  target 'ALSlidingNavigationBar_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.0.0'
  pod 'Nimble', '~> 5.1.1'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
  end
end
